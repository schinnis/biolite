{% include 'hero' %}

{% comment %}
  Need to add in instructions
{% endcomment %}

<section class="section-xlarge">
  <div class="wrapper">
    <div class="grid">
      <div class="grid__item">
          {% comment %}
          Regular page content goes here.
          {% endcomment %}
        <div class="rte">
          {{ page.content }}
        </div>
      </div>
    </div>
  </div>
</section>
